# Official TaxAce Logo Verification

The supplied official TaxAce logo is the approved runtime brand asset for this application.

The application must render the file without cropping, distortion, recoloring, recreation, or replacement typography. Shell placement uses `object-fit: contain` with width constraints and automatic height so the original aspect ratio is preserved.

Repository path:

`client/public/brand/taxace-logo.png`
